{
  "name": "spectre_puzzle",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "vite dev --host 0.0.0.0 --port 4487",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "vite preview --host 0.0.0.0 --port 4487",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vitest": "^4.1.7"
  }
}
